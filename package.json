{
  "name": "baremail",
  "version": "0.1.0",
  "description": "Email's bare necessities — a minimal Gmail PWA for low-bandwidth environments",
  "type": "module",
  "scripts": {
    "start": "node build.js --start",
    "build": "node build.js",
    "watch": "node build.js --watch",
    "dev": "node build.js --serve",
    "setup": "node scripts/setup.js"
  },
  "dependencies": {
    "dompurify": "^3.3.3",
    "htm": "^3.1.1",
    "minisearch": "^7.1.1",
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  },
  "license": "MIT"
}
